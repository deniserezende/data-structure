#ifndef via_operations_h
#define via_operations_h

#include "graph.h"
#include "vertex.h"
#include "edge.h"

// Reads and interprets the viafile.
void get_via_input(char via_fullpath[], type_graph via_graph, double view_box[4]);

#endif